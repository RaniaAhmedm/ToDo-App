import 'package:flutter/material.dart';

class TapSettings extends StatelessWidget {
  static const String routeName='settings';
  @override
  Widget build(BuildContext context) {
    return Container(
    );
  }
}
